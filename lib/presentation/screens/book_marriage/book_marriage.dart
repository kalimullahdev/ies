import 'package:flutter/material.dart';

class BookMarriageScreen extends StatelessWidget {
  const BookMarriageScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BookMarriageScreen'),
      ),
      body: const Center(
        child: Text('BookMarriageScreen'),
      ),
    );
  }
}
